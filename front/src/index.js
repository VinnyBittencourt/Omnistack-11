import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root")); // Onde renderiza no index.html o conteudo jsx (Javascript e XML (Que é a sintaxe do HTML)) que está no arquivo App.js
